{
  "originalGoal": "Fix all CI failures on PR #13. Clippy fails with 'use of unwrap_or_else to construct default value'. Security audit fails on upstream deps (not actionable, but check if there's an audit.toml to ignore known vulns). Fix clippy, ensure cargo fmt --check and cargo test --workspace pass, push the fix.",
  "hardenedTask": {
    "goal": "Fix all CI failures on PR #13. Clippy fails with 'use of unwrap_or_else to construct default value'. Security audit fails on upstream deps (not actionable, but check if there's an audit.toml to ignore known vulns). Fix clippy, ensure cargo fmt --check and cargo test --workspace pass, push the fix.",
    "expandedGoal": "Fix all CI failures on PR #13. Clippy fails with 'use of unwrap_or_else to construct default value'. Security audit fails on upstream deps (not actionable, but check if there's an audit.toml to ignore known vulns). Fix clippy, ensure cargo fmt --check and cargo test --workspace pass, push the fix. Work inside /home/drew/code/openclaw-sandbox-blueprint. Complete the full scope to production quality. Treat review, formatting, linting, and validation as part of completion.",
    "successCriteria": [
      "cargo clippy --workspace --all-targets --all-features -- -D warnings passes",
      "cargo fmt --all -- --check passes",
      "cargo test --workspace passes",
      "work is grounded in actual repository state",
      "completion is backed by executed checks or explicit blocker evidence"
    ],
    "checkCommands": [
      "cargo fmt --check",
      "cargo clippy --workspace",
      "cargo test --workspace",
      "cargo fmt --all -- --check",
      "cargo clippy --workspace --all-targets --all-features -- -D warnings",
      "cd /home/drew/code/openclaw-sandbox-blueprint && ./scripts/ci/run-docker-integration-tests.sh"
    ],
    "executionNotes": [
      "Run ALL check commands before declaring done, including formatting and lint.",
      "If checks fail, fix the issues — do not skip them.",
      "If the task is ambiguous, preserve the original goal and add inferred criteria separately."
    ],
    "inferred": true
  },
  "checkCommands": [
    "cargo fmt --check",
    "cargo clippy --workspace",
    "cargo test --workspace",
    "cargo fmt --all -- --check",
    "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "cd /home/drew/code/openclaw-sandbox-blueprint && ./scripts/ci/run-docker-integration-tests.sh",
    "cd /home/drew/code/openclaw-sandbox-blueprint && cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "cd /home/drew/code/openclaw-sandbox-blueprint && cargo fmt --all -- --check",
    "cd /home/drew/code/openclaw-sandbox-blueprint && cargo test --workspace"
  ],
  "toolCommands": [],
  "selectedWorkerId": "claude",
  "reviewWorkerId": "codex",
  "plannerWorkerId": "codex",
  "sandboxMode": "local",
  "promptVariantIds": {
    "hardener": "engineering-hardener:contract-v1",
    "implementer": "engineering-implementer:operator-v1",
    "reviewer": "engineering-reviewer:auditor-v1"
  },
  "promptPolicyMode": "explicit"
}
