{
  "done": true,
  "status": "completed",
  "reason": "[deterministic] all deterministic checks passed for engineering-openclaw-sandbox-blueprint | [judge] [fallback from codex] On-chain job submission flow is fully wired and production-quality. All four lifecycle operations submit transactions via useSubmitJob with correct ABI struct tuple encoding matching Rust sol! definitions. Destructive actions use confirmation dialog. Transaction states are comprehensive (signing/pending/confirmed/failed/stale recovery). Both build gates pass cleanly."
}
